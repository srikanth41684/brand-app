/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "bootstrap-ecommerce-web.netlify.app",
      "i.dummyjson.com",
      "lh3.googleusercontent.com",
    ],
  },
};

module.exports = nextConfig;
